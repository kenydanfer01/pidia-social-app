{% import 'theme/form.html.twig' as frm %}

{{ frm.start(form) }}
    {{ frm.columnX(
        [
            frm.row(form.nombre),
            frm.row(form.formato),
            frm.row(form.alias)
        ],
        6
    ) }}
    {{ frm.button(button_label|default('Guardar')) }}
{{ frm.end(form) }}

