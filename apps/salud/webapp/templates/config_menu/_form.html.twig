{% import 'theme/form.html.twig' as frm %}

{{ form_start(form) }}
    {{ frm.column2(
        frm.row(form.name),
        frm.row(form.route)
    ) }}
    {{ frm.button(button_label|default('Guardar')) }}
{{ form_end(form) }}
