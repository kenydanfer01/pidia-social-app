{% extends 'base.html.twig' %}
{% import '@App/theme1/breadcrumb.html.twig' as breadcrumb %}
{% import '@App/theme1/pagination.html.twig' as pagination %}

{% set _title = 'Ficha Evaluación' %}
{% set _main_title = 'Listado' %}
{% set _section =  'ficha_evaluacion_index' %}

{% block _breadcrumb %}
    {{ breadcrumb.show(_title) }}
{% endblock %}

{% block _main %}
    {{ pagination.pager(paginator, _section) }}
    {% embed 'theme/_table.html.twig' %}
        {% block table_head %}
            {% import 'theme/action.html.twig' as action %}
            {% import '@App/theme1/table.html.twig' as table %}

            <tr>
                {{ table.th('#') }}
                {{ table.th('Datos', true, 'd-block d-sm-none') }}
                {{ table.th('Paciente', true, 'd-none d-sm-table-cell') }}
                {{ table.th('Diagnóstico', true, 'd-none d-sm-table-cell') }}
                {{ table.th('Activo', true, 'd-none d-sm-table-cell text-center') }}
                <th></th>
            </tr>
        {% endblock %}
        {% block table_body %}
            {% import 'theme/action.html.twig' as action %}
            {% import '@App/theme1/table.html.twig' as table %}

            {% for ficha_evaluacion in paginator.results %}
                <tr>
                    <td class="text-center">{{ loop.index | index (paginator)}}</td>
                    <td class="d-block d-sm-none">
                        <table class="table gs-0 gy-0">
                            {{ table.tbl_tr_th_td('Paciente', ficha_evaluacion.paciente) }}
                            {{ table.tbl_tr_th_td('Diagnóstico', ficha_evaluacion.diagnostico) }}
                            {{ table.tbl_tr_th_td('Activo', ficha_evaluacion.isActive  | yesno_custom) }}
                        </table>
                    </td>
                    <td class="d-none d-sm-table-cell">{{ ficha_evaluacion.paciente }}</td>
                    <td class="d-none d-sm-table-cell">{{ ficha_evaluacion.diagnostico }}</td>
                    <td class="d-none d-sm-table-cell text-center">{{ ficha_evaluacion.isActive | yesno_custom | raw }}</td>
                    <td class="">
                        <div class="float-end">
                            {% if access.has('show', ficha_evaluacion) %}
                                {{ action.btn_show_link2_ico(path('ficha_evaluacion_show', {'id': ficha_evaluacion.id})) }}
                            {% endif %}
                            {% if access.has('edit', ficha_evaluacion) %}
                                {{ action.btn_edit_link2_ico(path('ficha_evaluacion_edit', {'id': ficha_evaluacion.id})) }}
                            {% endif %}
                        </div>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4">No hay registros</td>
                </tr>
            {% endfor %}
        {% endblock %}
    {% endembed %}

    {{ pagination.show(paginator, _section) }}
{% endblock %}

{% block _main_footer %}
    {% include 'crud/_index_links.html.twig' with {'route_base': 'ficha_evaluacion'} %}
{% endblock%}

{% block _body_tools %}
    {% include 'crud/_index_links_ico.html.twig' with {'route_base': 'ficha_evaluacion'}  %}
{% endblock %}

{% block stylesheets %}
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('js/crud/list.js') }}"></script>
    <script>
        $(document).ready(function () {
            CRUDList.init('{{ path('ficha_evaluacion_index') }}');
        });
    </script>
{% endblock %}
