{% import 'theme/form.html.twig' as frm %}

{{ frm.start(form) }}
    {{ frm.columnX(
        [
            frm.row(form.fichaEvaluacion),
            frm.row(form.examenAuxiliar)
        ],
        6
    ) }}
    {{ frm.columnX(
        [
            frm.row(form.detalles)
        ],
        12
    ) }}<br>
    {{ frm.button(button_label|default('Guardar')) }}
{{ frm.end(form) }}

