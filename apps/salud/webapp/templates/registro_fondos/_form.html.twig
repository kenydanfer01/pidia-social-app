{% import 'theme/form.html.twig' as frm %}

{{ frm.start(form) }}
    {{ frm.columnX(
        [
            frm.row(form.dni),
            frm.row(form.nombreSocio),
            frm.row(form.baseSocial),
            frm.row(form.tipoSocio),
            frm.row(form.tipoRegistro),
            frm.row(form.fecha),
            frm.row(form.monto),
            frm.row(form.condicion),
            frm.row(form.observacion),
        ],
        6
    ) }}
    {{ frm.button(button_label|default('Guardar')) }}
{{ frm.end(form) }}
