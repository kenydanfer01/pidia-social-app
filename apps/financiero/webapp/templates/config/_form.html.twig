{% import 'theme/form.html.twig' as frm %}

{{ form_start(form) }}
    {{ frm.column2(
            frm.row(form.alias),
            frm.row(form.nombre)
    ) }}
    {{ frm.column(form.menus, 2) }}
    {{ frm.button(button_label|default('Guardar')) }}
{{ form_end(form) }}
