<form method="post" action="{{ path('app_tipo_soporte_delete', {'id': tipo_soporte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_soporte.id) }}">
    <button class="btn">Delete</button>
</form>
